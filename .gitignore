# Ignore everything inside the "save" directory
save/
# Except the "example" directory and readme.md
!save/example/
!save/readme.md

# Ignore everything inside the "services" directory
services/*
# Except the "example" directory and readme.md
!services/example/*
!services/readme.md

# Keep the .gitkeep itself everywhere, including subdirectories
!**/.gitkeep

restore.sh